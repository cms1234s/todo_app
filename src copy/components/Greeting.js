import React from 'react';

const Greeting = (props) => {
   
    
   return(
    <div>
        <h1>Temp Calc</h1>
      {/* <h1>Hello,{props.isLoggedIn ? 'User' : props.role} from Greeting. your age is {props.age}</h1>
      <button onClick = {props.addition(a,b)}>Log In</button>
      <hr></hr>
      <button onClick = {props.logOutHandler}>Log Out</button> */}
    </div>
  )
}

export default Greeting;


